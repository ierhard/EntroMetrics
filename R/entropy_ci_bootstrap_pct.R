entropy_ci_bootstrap_pct <- function(bin_counts,
                                     pt_est_fct,
                                     B = 10^3,
                                     conf_level,
                                     pre_calc_bootstraps){

  return("TO WRITE: entropy_ci_bootstrap_pct()")

}
